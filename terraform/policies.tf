/*
  policies.tf (disabled)

  Organization-level policies have been removed from the active Terraform
  run to avoid requiring management account permissions during a lightweight
  demo. Re-enable and parameterize these when performing cross-account
  organization bootstrapping with a dedicated management credential.

*/